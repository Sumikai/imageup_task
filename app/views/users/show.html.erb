<div class="row">
  <div class="col-xs-12 col-md-6 col-md-offset-3">

    <h1>登録情報</h1>
    
    <p><%= notice %></p>
    
      <p><%= image_tag @user.image.url, class: "user-img" %></p>
      <p>Name: <%= @user.name %></p>
      <p>Mail: <%= @user.email %></p>
    
    <% if logged_in? %>
      <p><%= link_to "編集", edit_user_path(current_user.id) %></p>
    <% end %>
  </div>
</div>